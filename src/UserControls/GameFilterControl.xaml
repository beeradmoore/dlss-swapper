<UserControl
    x:Class="DLSS_Swapper.UserControls.GameFilterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DLSS_Swapper.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock Text="{x:Bind ViewModel.TranslationProperties.OptionsText, Mode=OneWay}" Style="{ThemeResource BaseTextBlockStyle}" />
        <CheckBox Content="{x:Bind ViewModel.TranslationProperties.HideGamesWithNoSwappableItemsText, Mode=OneWay}" IsChecked="{x:Bind ViewModel.HideNonSwappableGames, Mode=TwoWay}" />
        <CheckBox Content="{x:Bind ViewModel.TranslationProperties.ShowHiddenGamesText, Mode=OneWay}" IsChecked="{x:Bind ViewModel.ShowHiddenGames, Mode=TwoWay}" />
        
        <TextBlock Text="{x:Bind ViewModel.TranslationProperties.GroupingText, Mode=OneWay}" Style="{ThemeResource BaseTextBlockStyle}"  Margin="0,10,0,0" />
        <CheckBox Content="{x:Bind ViewModel.TranslationProperties.GroupGamesFromTheSameLibraryTogetherText, Mode=OneWay}" IsChecked="{x:Bind ViewModel.GroupGameLibrariesTogether, Mode=TwoWay}" />
    </StackPanel>
</UserControl>
